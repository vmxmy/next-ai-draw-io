# 说明：
# - 这是环境变量模板文件（可提交到仓库）。
# - 本地开发请复制为 `.env.local` 并填写真实值（`.env.local` 默认会被 git 忽略）。
# - 不要把真实密钥提交到 git 或粘贴到聊天/工单中。

# =========================
# NextAuth（仅 OAuth）+ Postgres
# =========================

# 应用对外访问 URL：
# - 本地： http://localhost:6002
# - 生产： https://drawio.ziikoo.com
NEXTAUTH_URL=http://localhost:6002

# NextAuth 加密签名密钥（自行生成强随机字符串）
# 生成示例：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=your_nextauth_secret

# Postgres 连接串（自部署数据库）
# 示例：postgresql://user:password@host:5432/dbname?schema=public
DATABASE_URL=postgresql://user:password@localhost:5432/next_ai_drawio?schema=public

# GitHub OAuth（在 GitHub OAuth App 创建后获取）
GITHUB_ID=your_github_client_id
GITHUB_SECRET=your_github_client_secret

# =========================
# 现有 AI Provider 配置（节选）
# 需要完整配置项请参考 `env.example`
# =========================

# AI_PROVIDER=bedrock
# AI_MODEL=global.anthropic.claude-sonnet-4-5-20250929-v1:0

# Draw.io Configuration (Optional)
# NEXT_PUBLIC_DRAWIO_BASE_URL=https://embed.diagrams.net

